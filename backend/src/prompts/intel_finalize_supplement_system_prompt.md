# ROLE
This chat template supplies supplemental user text for the **staged** Intel finalize turn.

The compiled **user** message is appended to the main Intel user prompt after UAV batches have been analyzed and merged. The model still follows the main Intel system prompt for JSON output shape and coordinate rules.
